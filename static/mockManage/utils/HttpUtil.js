const CTX = window.location.origin;

export default {
  //======================================
  CREATE_WORKPLACE: `${CTX}/api/createWorkplace`,

  //======================================
  GET_WP_List: `${CTX}/api/wpList`
};
